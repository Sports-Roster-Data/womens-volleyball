# Core dependencies for NCAA Women's Volleyball Roster Scraper
requests>=2.31.0
beautifulsoup4>=4.12.0
requests-html>=0.10.0
tldextract>=5.0.0
lxml>=4.9.0
lxml-html-clean>=0.2.0

# JavaScript rendering (requires browser installation)
shot-scraper>=1.0.0
playwright>=1.40.0

# Note: After installing dependencies, run:
#   shot-scraper install
# or:
#   playwright install chromium
#
# This will download the required browser binaries (~200MB)
